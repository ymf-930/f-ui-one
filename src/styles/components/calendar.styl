.fei-calendar {
  padding: 12px 20px 35px;
  width: 100%;
  height: 100%;
  box-sizing: border-box;
  ul > li {
    list-style: none;
    margin: 0;
  }
  .fei-calendar-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 12px 20px;
    border-bottom: 1px solid #ebeef5;
    &-title {
      color: var(--fei-color-text-primary);
      align-self: center;
      font-size: var(--fei-font-size-default);
    }
  }
  .fei-calendar-week {
    display: flex;
    width: 100%;
    &-item {
      flex: 1;
      text-align: center;
      font-size: var(--fei-font-size-default);
      color: var(--fei-color-text-default);
      line-height: var(--fei-base-line-height);
      font-weight: 500;
      padding: 10px 0;
    }
  }
  .fei-calendar-body {
    display: flex;
    flex-wrap: wrap;
    width: 100%;
    border-top: var(--fei-border-base);
    border-left: var(--fei-border-base);
  }
  .fei-calendar-date-view {
    width: 14.285%;
    height: 80px;
    line-height: var(--fei-base-line-height);
    padding: 8px;
    border-right: var(--fei-border-base);
    border-bottom: var(--fei-border-base);
    cursor: pointer;
    &.is-current-day, &.is-selected {
      color: var(--fei-color-primary);
    }
    &.is-selected, &:hover {
      background-color: var(--fei-color-primary-light5);
    }
    &.prev, &.next {
      color: var(--fei-color-text-disabled);
    }
  }
  &.card {
    width: 300px;
    .fei-calendar-header {
      padding: 10px;
    }
    .fei-calendar-date-view {
      padding: 0;
      height: 34px;
      line-height: 34px;
      text-align: center;
    }
  }
}
